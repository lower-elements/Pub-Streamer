# Core (always required)
pyaudiowpatch>=0.2.12
numpy>=1.24
pedalboard>=0.9
pywin32>=306
httpx>=0.27
wxPython>=4.2

# TTS engines (install what you need; the rest will show as unavailable)
websockets>=12.0          # Star TTS server
espeakng>=1.0             # eSpeak-NG (requires espeak-ng shared lib)
piper-tts>=1.2            # Piper neural TTS (bundles onnxruntime)
azure-cognitiveservices-speech>=1.38  # Azure TTS
google-cloud-texttospeech>=2.16       # Google Cloud TTS
boto3>=1.34                           # AWS Polly
